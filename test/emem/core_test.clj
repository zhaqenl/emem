(ns emem.core-test
  (:require [clojure.test :refer :all]
            [emem.core :refer :all]))

;; (deftest a-test
;;   (testing "FIXME, I fail."
;;     (is (= 0 1))))

(deftest a-test
  (testing "blah"
    (is (= 0 0))))
